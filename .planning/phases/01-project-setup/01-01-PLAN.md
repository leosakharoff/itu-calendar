# Plan 01-01: Vite + React + TypeScript Scaffold

## Objective
Set up the project foundation with Vite, React, and TypeScript. Establish folder structure for the calendar app.

## Context
- @.planning/BRIEF.md — project vision and requirements
- Greenfield project in `/Users/leosakharov/Dev/itu-calendar/`

## Tasks

### Task 1: Scaffold Vite + React + TypeScript
- **Type**: setup
- **Action**: Run `npm create vite@latest . -- --template react-ts` in project root
- **Verify**: `npm run dev` starts dev server without errors
- **Done**: Dev server runs at localhost

### Task 2: Clean up boilerplate
- **Type**: edit
- **Files**: `src/App.tsx`, `src/App.css`, `src/index.css`
- **Action**:
  - Remove Vite demo content from App.tsx
  - Clear App.css and index.css
  - Add minimal App component that renders "ITU Calendar"
- **Verify**: Page shows "ITU Calendar" text only
- **Done**: Clean slate ready for calendar components

### Task 3: Create folder structure
- **Type**: setup
- **Action**: Create directories:
  ```
  src/
  ├── components/    # React components
  ├── hooks/         # Custom hooks
  ├── lib/           # Utilities, Supabase client
  ├── types/         # TypeScript types
  └── styles/        # CSS files
  ```
- **Verify**: Directories exist
- **Done**: Folder structure in place

## Verification
- [ ] `npm run dev` works
- [ ] Browser shows "ITU Calendar"
- [ ] All folders created under `src/`
- [ ] No TypeScript errors

## Success Criteria
Project scaffolded with clean structure, ready for Supabase integration in next plan.

## Output
Create `01-01-SUMMARY.md` with:
- Commands run
- Final folder structure
- Dev server URL
- Any issues encountered
